import { LetraCabeceraDirective } from './letra-cabecera.directive';

describe('LetraCabeceraDirective', () => {
  it('should create an instance', () => {
    const directive = new LetraCabeceraDirective();
    expect(directive).toBeTruthy();
  });
});
